# 仅提升 ESLint/Prettier 相关依赖到根 node_modules（兼容性优先，但保留 pnpm 优势）
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*
public-hoist-pattern[]=*typescript*

# 其他优化选项
strict-peer-dependencies=false  # 避免 peerDependencies 版本冲突时安装失败
prefer-frozen-lockfile=true     # 优先复用 lockfile（CI 环境推荐）